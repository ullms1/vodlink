# ─────────────────────────────────────────────────────────────────────────────
# VodLink configuration
# Copy this file to .env and fill in the values for your environment.
# ─────────────────────────────────────────────────────────────────────────────

# ── VodLink UI ───────────────────────────────────────────────────────────────
# Port the VodLink web interface is available on
VODLINK_PORT=7842

# ── VOD source (read-only) ───────────────────────────────────────────────────
# Host path to the VOD2MLIB output directory (contains Movies/ and Series/).
# Mounted read-only into the container at /vod/src.
VOD_SRC=/volume1/docker/dispatcharr/VODS

# ── VOD destination (read-write) ─────────────────────────────────────────────
# Host path to the root directory Emby scans (contains Movies/ and Series/).
# Mounted read-write into the container at /vod/dest.
VOD_DEST=/volume1/SSD/VOD

# ── Data directory ───────────────────────────────────────────────────────────
# Where VodLink stores its SQLite database and backups.
DATA_DIR=/volume1/docker/vodlink/data

# ── User/group for file ownership ────────────────────────────────────────────
# UID/GID the container runs as. Files will be created with this ownership.
# Run: id <your-user>  to find your UID/GID.
PUID=1000
PGID=1000

# ── Timezone ─────────────────────────────────────────────────────────────────
# IANA timezone name for scheduled scans. Find yours at:
# https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York

# ── VodLink base URL ─────────────────────────────────────────────────────────
# URL Emby/Plex uses to reach this host. Written into .strm proxy files.
# Use the host IP address (not hostname) to avoid DNS resolution issues.
VODLINK_BASE_URL=http://192.168.1.60:7842

# ── Dispatcharr internal URL (optional) ──────────────────────────────────────
# Set when VOD2MLIB writes a different host into source .strm files than what
# the VodLink container can reach (e.g. host IP vs macvlan IP).
# Replaces the scheme+host+port in source URLs before proxying.
# DISPATCHARR_URL=http://192.168.40.80:9191

# ── Downloads directory (optional) ───────────────────────────────────────────
# Host path to save downloaded video files. Must exist before container starts.
# Leave blank to disable the Download feature. Mounted at /downloads in the
# container. Subdirectories Movies/ and Series/ are created automatically.
DOWNLOADS_DIR=/volume1/SSD/downloads
